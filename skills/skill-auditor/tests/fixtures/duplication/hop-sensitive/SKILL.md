---
name: hop-sensitive-demo
description: >-
  Fixture skill for duplication max-hop testing. Use when validating that
  reachable markdown becomes operative only after the configured hop budget.
---

# Hop Sensitive Demo

Read `<skills-file-root>/references/bridge.md` before finalizing the audit.

The auditor MUST emit a deterministic duplication summary.
