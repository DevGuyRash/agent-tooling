interface:
  display_name: "Rust Development"
  short_description: "Idiomatic, production-grade Rust development"
  default_prompt: "Use $rust-development for repo-adaptive Rust delivery: start with the compatibility map, choose Fast Path only for deterministic localized changes, use the phased Standard workflow otherwise, and enforce verification/bootstrap via scripts/verify.sh and scripts/scaffold.sh."
