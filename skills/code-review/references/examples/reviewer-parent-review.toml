schema_version = "mpcr_artifact.v1"
artifact_kind = "parent_review"
artifact_id = "44291378249e"
session_id = "88fc8ad3"
target_ref = "refs/heads/main"
producer_kind = "review-composite"
policy_version = "2026.03.08"
created_at = "2026-03-09T17:54:28Z"
confidence_label = "high"
confidence_score = 90
source_artifact_ids = ["73078a595848"]
final_verdict = "request_changes"
follow_up = []
residual_risks = []

[[loaded_policy_refs]]
category = "mode"
id = "reviewer"
version = "2026.03.08"
view = "checklist"

[[loaded_policy_refs]]
category = "worker"
id = "review-composite"
version = "2026.03.08"
view = "checklist"

[[loaded_policy_refs]]
category = "module"
id = "core-correctness"
version = "2026.03.08"
view = "checklist"

[[loaded_policy_refs]]
category = "module"
id = "ship-readiness"
version = "2026.03.08"
view = "checklist"

[ship_readiness]
verdict = "do_not_ship"
axes = ["correctness", "contract"]
blocking_items = [
  "Quantity handling under-reserves stock",
  "Success payload breaks the established contract",
]
required_now_count = 2
follow_up_count = 0

[coverage_summary]
changed_files = ["src/orders.js", "README.md"]
surfaces_covered = ["behavior-change", "docs-staleness", "public-api"]
modules_loaded = ["core-correctness", "docs-staleness", "ship-readiness"]
tests_run = []
tests_not_run_reason = "No automated tests were provided in the sample repository."
limitations = []

[[required_now]]
finding_id = "F001"
module_id = "core-correctness"
surface_ids = []
severity = "major"
title = "Quantity handling under-reserves stock"
claim = "The function decrements stock by one regardless of the requested quantity, so successful multi-quantity reservations leave inventory overstated."
scenario = "Reserve 3 units of a SKU with stock 5. The function reports success but leaves stock at 4 instead of 2."
evidence = "The changed line `remainingStock[item.sku] = available - 1` ignores `item.quantity`."
recommendation = "Subtract `item.quantity` and add a regression test that reserves more than one unit."
verification = "Call `reserveItems({ A: 5 }, { items: [{ sku: \"A\", quantity: 3 }] })` and confirm remaining stock is 2."
anchors = ["src/orders.js:16"]
symbol_hint = "reserveItems"
anchor_cluster = "src/orders.js#reserveItems"
fingerprint = "3d0e084bf49b9760"
reopen_eligible = true
confidence_label = "high"
confidence_score = 96

[[required_now]]
finding_id = "F002"
module_id = "core-correctness"
surface_ids = ["public-api"]
severity = "major"
title = "Success payload breaks the established contract"
claim = "The successful return shape changed from ok/reserved/remainingStock to status/items, which breaks callers built against the previous contract on refs/heads/main."
scenario = "A caller on the target branch checks `result.ok` and reads `result.remainingStock`; after this change both fields disappear on success."
evidence = "The old success object returned `ok`, `reserved`, and `remainingStock`, but the new code returns only `status` and `items`."
recommendation = "Preserve the previous fields or version the API change with coordinated caller updates."
verification = "Run an existing caller fixture from `refs/heads/main` against the new implementation and confirm its success-path assertions still pass."
anchors = ["src/orders.js:20-22"]
symbol_hint = "reserveItems"
anchor_cluster = "src/orders.js#reserveItems"
fingerprint = "c82d76798eca866b"
reopen_eligible = true
confidence_label = "high"
confidence_score = 92

[[defended_summary]]
check_id = "C001"
module_id = "docs-staleness"
surface_ids = ["docs-staleness"]
method = "readme-congruence"
claim = "The README matches the success payload emitted by the implementation."
attempted_counterexample = "Compared the success object documented in README.md with the object returned from `src/orders.js`."
evidence = "README.md states `reserveItems` returns `{ status: \"reserved\", items }` on success, and the implementation returns those exact fields."
anchors = ["README.md:5", "src/orders.js:20-22"]
confidence_label = "high"
confidence_score = 86
