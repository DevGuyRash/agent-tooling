interface:
  display_name: "Code Review"
  short_description: "Machine-first code review, application, and full-cycle convergence with mpcr v2"
  default_prompt: "Use $code-review to route first with `mpcr route`, then load only the selected `mpcr protocol mode/worker/module/escalation` packs, persist canonical artifacts, validate with hard and soft layers, render markdown from artifacts only, and drive `mpcr reviewer`, `mpcr applicator`, `mpcr fullcycle`, and `mpcr session metrics` flows through the v2 CLI."
