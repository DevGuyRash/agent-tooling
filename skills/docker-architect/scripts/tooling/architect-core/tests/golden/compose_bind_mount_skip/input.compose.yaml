services:
  api:
    image: nginx:1.27
    user: "101:101"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - ./host-data:/var/cache/app
