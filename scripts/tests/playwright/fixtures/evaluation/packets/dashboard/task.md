# Inspect Router details and saved access

The inventory team needs read-only evidence of the Router devices in North Operations and South Research. Use the synthetic reviewer session, distinguish the devices by their asset IDs, and inspect their details against the product brief. Capture enough context to make any material mismatch understandable.

Also establish whether the saved reviewer session can reopen the workspace in a fresh browser context without another login. Keep the original and recovered observations distinguishable. Your deliverable is usable evidence, the scripts or test changes needed to reproduce it, and a concise account of the supported findings and any limitations.

Use `reviewer@example.test` at the demo login. It has no password and grants access only to this synthetic instance. You MAY inspect the rendered interface, ordinary application requests and responses, and public client code; the inventory remains unchanged. The assignment supplies the local application origin, Playwright installation, and your output directory. `product-brief.md` is the legitimate product input.
