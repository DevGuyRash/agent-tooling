# Bounded implementation qualification

The release requirements and actual task inputs govern assessment; candidate skills are treatments, never graders or release authorities. The primary agent and Pro inspect original outcomes and contracts. This is a qualification of bounded uses, not a superiority claim, and it does not add an incumbent/no-guidance campaign to the completed access pilot.

Use serial temporary ChatGPT contexts with the complete current package supplied as files. Record visible model, effort, temporary/personalization state, supplied file hashes, native responses, assistance, and failures. Account personalization and platform behavior remain uncontrolled. An explicit skill request does not demonstrate implicit discovery. Preserve every admitted run; do not replace an unfavorable valid outcome. Correct a demonstrated instrument defect prospectively and retain its original outcome.

Cases cover an ordinary prompt and context arrangement, a follow-up narrowing into a new self-contained skill, a narrow edit, sound and defective audit targets, and a comparison requiring fresh executable outcomes. The authored skill then runs downstream in a separate context with legitimate event notes but without this package or authoring history. Several concerns share one realistic case; no fixed scoring schema or quality wording assertions are used.

Assess whether the actual requested work is correct, scoped, usable, and supported by original artifacts. Preserve unsupported mechanisms and generalizations as limits. These trials do not prove native Codex/Claude routing, file isolation, compaction behavior, exact backend identity, or comprehensive effectiveness.

The initial four packets were frozen before task responses. The comparison's first execution uses the prospectively revised v2 packet after Pro's concrete implementation review; no v1 comparison was admitted. The first authoring, continuation, downstream, narrow-edit, and audit tasks use their retained v1 packets. Exact source identities are in the per-packet inventories; small later source changes are not retrospectively attributed to earlier observations. Browser trials use the user-authorized xhigh setting: Latest was selected, Extra High (4/5) was visible at sends, and each chat's Personalized temporary state was verified after hydration. Exact backend identity and all ambient influences remain unverified.
