# 1.1.0 delivery record

The package was published on `main` in [e64fb5825e498208f1eb5e6af580f544fc0efba0](https://github.com/DevGuyRash/agent-tooling/commit/e64fb5825e498208f1eb5e6af580f544fc0efba0). [GitHub CI](https://github.com/DevGuyRash/agent-tooling/actions/runs/35071930828) and the [sensitive scan](https://github.com/DevGuyRash/agent-tooling/actions/runs/35071930895) succeeded for that commit.

The selected update used `scripts/install-all --include agentic-design-and-evaluation` with the canonical GitHub marketplace. Both Codex and Claude report the enabled 1.1.0 installation. All 41 package files and their executable availability match the published source in both caches. Claude's strict validator accepts the installed package. Fresh Codex prompt input exposes all four new descriptions and the selected cache location; this is catalog exposure without native model execution.

Comparison with the saved pre-update inventory preserves all previously exposed fields of 23 unrelated Codex installations and 19 unrelated Claude installations. Protected raw Claude registration metadata, unrelated plugin enablement, and unrelated host configuration also remain unchanged. Private inventories, source hashes, and verification commands remain in the local task record rather than publishing unrelated workstation state.

## Receipt reconciliation

The first dry run stopped because the Codex cache no longer matched the installer's 1.0.0 receipt. Full-tree verification established that the cache exactly matched published revision `92a2eebf746ef39d631b7d2b20b7fb574c39682f`, including its foundation table formatting and note-metadata cleanup. Claude still matched the original 1.0.0 publication. The producer of the Codex cache change was not established.

The Codex cache and old receipt were backed up before reconciling only that selected receipt entry under the installer's lifecycle lock. The reconciled identity remained version 1.0.0 and recorded its observed, published content; it did not claim that 1.1.0 had been installed. The ordinary selected update then succeeded without changing the installer, bypassing its drift check for unknown content, forcing a reinstall, or replacing the marketplace source.

Existing sessions can retain previously loaded instructions. A fresh session uses the updated catalog and package. This delivery record establishes the observed update and preservation boundaries, not future health or native model behavior.

A subsequent invocation of the same selected installer returned `all selected plugins are current`. Cleanup moved four conversion staging directories, the six-packet transport directory, and two redundant downloaded artifacts to system trash. All 344 files were verified against their original content and modes there; the trash was not emptied. Qualification inputs, outputs, and the installation-preservation records remain retained.
