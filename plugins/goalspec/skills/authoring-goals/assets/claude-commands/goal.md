---
description: Execute a frozen GoalSpec mission. Paste the rendered launch line after /goal — it points at the locked contract or campaign and the focus cursor.
argument-hint: [rendered launch line]
---

$ARGUMENTS
