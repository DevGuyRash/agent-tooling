# In flight

- Qualify the Playwright runtime on native Windows, including private-desktop popup affinity, foreground behavior, command/argument handling and Job Object teardown; use the shipped qualification command and platform reference.
- Configure and qualify an isolated native macOS session provider, including actual rendering OS, connection compatibility, teardown and owner-loss expiry.
- Resolve Excel Foundry’s missing `references/query.md` and `references/task-router.md` links from `references/usage.md`.
- Complete GoalSpec’s live decision-funnel evaluation across Codex and Claude, covering design clarity, convergence, artifact restraint, probe quality, default handling and product regressions.
- Qualify Excel Foundry’s opt-in Graph/Fabric/Power BI commands with tenant credentials and suitable test resources before promoting cloud surfaces to supported.
- Resolve the delivery choice for Claude `agents/*.md` surfaces that remain preserved-only in Claude-to-Codex conversion.
- Reconcile the MIT declarations with the missing package license files in `plugins/excel-foundry/` and `plugins/goalspec/`.
- Complete Software Development’s model-backed trigger and no-plugin baseline evaluations and its PowerShell/Swift/Kotlin runtime acceptance checks.
