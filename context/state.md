- Excel Foundry overhaul is still open in two places: compatibility analysis is heuristic rather than a true Excel compatibility checker, and package-backed write coverage still stops short of pivots, Power Query, connections, and model artifacts even though workbook metadata, dimensions, hyperlinks, comments, print settings, and existing-table updates now sync safely for package-readable workbooks.
- The repo rename from `skills/excel-workbook-sync/` to `skills/excel-foundry/` is still in flight; keep compatibility references aligned until the old path is fully removed from docs, tests, helper entrypoints, and any external consumers.
