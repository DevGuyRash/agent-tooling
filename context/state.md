Active risks:

- GoalSpec live-fire gauntlet (`plugins/goalspec/tests/live-fire-gauntlet.md`): Codex-side LF-1x/LF-2x done after the multi_agent_v2 unblock. Open: LF-3 (three owner-selected real tasks through the full lifecycle), and confirm whether Codex PreToolUse can fire at all under `approval_policy = never` + danger-full-access (evt-0174) — until then the contract freeze on Codex is detect-only.
- GoalSpec fixes committed locally but not deployed (fea1840, fca3d69, e0ec5a2; plugin at 1.0.1): both marketplaces track GitHub `DevGuyRash/agent-tooling@main` — push, then `scripts/install-all --include goalspec`; sessions and Codex runs before that keep executing the old hooks (the LF-x probes ran on 1.0.0).
- GoalSpec boundedness mechanism chosen and shipped: `launch_goal.py` (external wall-clock ceiling + verifiers + audit at close) is the supported unattended launch path, wired into SKILL.md. Remaining frame-review decision: adopt it habitually for real runs (LF-3) and restate the deliverable framing (refusal outputs are first-class).
- GoalSpec fleet evt-0178/0179 residue: `record_provenance.py` exists but executors hand-author provenance and drift; consider a SKILL.md nudge only if it recurs in LF-3 (skip otherwise — diminishing returns).
- Friction diagnostics helper is hanging on this Windows event stream; do not leave stale `.report-friction.lock` behind after attempts to log.
- Excel Foundry cloud commands still need opt-in live Graph/Fabric/Power BI validation with tenant env vars and safe test resources before any cloud surface is promoted to supported.
- Claude `agents/*.md` surfaces are now reported as `preserved_only` in claude→codex conversion but still have no Codex mapping; decide whether a Codex-side agent equivalent should exist or whether preserved-only is the end state.
