Active risks:

- GoalSpec LF-3 still open: the first real-task run end-to-end, now best done as a real campaign chain (authoring agent produces the manifest + children, owner pastes the rendered `--campaign` `/goal`). 1.6.0 (shift-left: attestation-advance/pause chain semantics, review anchor + per-child verdicts, vendored `.goals/bin` runtime, decision-register harvest, reviewer shipped on both surfaces, host-thread-goal handoff boundary) is pushed and deployed to both CLI caches; sessions opened before the install still run older hooks until restarted. The chain remains the least field-tested load-bearing feature; every e2e so far was synthetic. LF-3 should now prove on a real request: source-grain near wave, an attestation pause/ratify/relaunch cycle, an anchored review produced by the shipped reviewer agent, decision-register items routed to conditional children, and a handoff that ends at the launch line (no host thread goal). The frozen EdgeCourt campaign is the standing regression fixture: 1.6.0 validate/status must keep flagging its 15 pause points and unanchored review.
- Codex PreToolUse (evt-0174): now observed firing AND denying live (2026-06-11, goalspec 1.2.0, `bypassPermissions`, EdgeCourt authoring run) — the freeze enforced on that runtime, contradicting the 2026-06-10 detect-only observation on 0.138.0. Coverage varies by runtime/version: keep `conformance_probe.py observe` on new environments and treat render-refuse + audit hash as authoritative either way.
- GoalSpec doctrine candidate for the next SKILL.md touch: prefer independent reviewer-agent gates over human gates for subjective verifiers (full hands-off by default); reserve human gates for explicit owner veto. Also: watch whether hand-authored provenance drift (evt-0178/0179) recurs in LF-3 before adding any nudge.
- Friction diagnostics helper is hanging on this Windows event stream; do not leave stale `.report-friction.lock` behind after attempts to log.
- Excel Foundry cloud commands still need opt-in live Graph/Fabric/Power BI validation with tenant env vars and safe test resources before any cloud surface is promoted to supported.
- Claude `agents/*.md` surfaces are now reported as `preserved_only` in claude→codex conversion but still have no Codex mapping; decide whether a Codex-side agent equivalent should exist or whether preserved-only is the end state.
